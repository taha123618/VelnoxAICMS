export enum DeviceType {
    Desktop = 'desktop',
    Tablet = 'tablet',
    Mobile = 'mobile',
}

export enum CurrentState {
    Default = 'default',
    Hover = 'hover',
}

export enum InsertLocation {
    Before = 'before',
    After = 'after',
}

export enum BackgroundType {
    Classic = 'classic',
    Gradient = 'gradient',
}

export enum CSSPositions {
    Relative = 'relative',
    Absolute = 'absolute',
    Fixed = 'fixed',
    Static = 'static',
    Sticky = 'sticky',
}

export enum InputTypes {
    Text = 'text',
    Textarea = 'textarea',
    Email = 'email',
    Radio = 'radio',
    Checkbox = 'checkbox',
    CheckboxGroup = 'checkbox-group',
    Select = 'select',
    MultiSelect = 'multi-select',
    Number = 'number',
    Password = 'password',
    Date = 'date',
    // Hidden = 'hidden',
}
